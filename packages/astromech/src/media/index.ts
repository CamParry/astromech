/** `media` domain module — media CRUD service + image serving pipeline. */
export { mediaApi } from './service.js';
export { mediaDescriptors } from './descriptors.js';
export { updateMediaSchema } from './schema.js';
