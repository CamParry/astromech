---
interface Props {
    body?: string | null;
}

const { body } = Astro.props;
---

{body && (
    <section class="max-w-3xl mx-auto px-4 py-12">
        <div
            class="prose prose-gray max-w-none"
            set:html={body}
        />
    </section>
)}
