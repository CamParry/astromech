# Prettier already honours .gitignore (dist/, coverage/, .wrangler/, generated
# routeTree.gen.ts, apps/demo/database.db, **/.astromech/). Only additions go
# here.

# Untracked, per-developer Claude Code settings — not repo content.
.claude/settings.local.json

# Lockfile: pnpm owns the formatting.
pnpm-lock.yaml

# Migration snapshot: db:generate owns the formatting. Prettier collapses its
# expanded arrays and the next generate expands them again.
apps/demo/migrations/snapshot.json
