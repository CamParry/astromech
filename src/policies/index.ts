// policies — composable wrappers over service methods: permissions, visibility,
// confirmation. Rules live here, not in a separate "domain" layer. Wraps services;
// never imported by services. See specs/services-architecture.md §3.
//
// Barrel stub (Stage 0 scaffold). Populated in Stage 1 (visibility/permissions) + Stage 4.
export {};
