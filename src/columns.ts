/**
 * `astromech/columns` — admin list-view column factories.
 *
 * Pure functions only (no module singletons), safe across tsup entry chunks.
 * Intended for namespaced use: `import * as column from 'astromech/columns'`.
 */

export * from '@/builders/columns.js';
