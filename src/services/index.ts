// services — the capability layer: service methods (entries/media/users/settings)
// that call storage and have policies composed onto them. Internal & private —
// see specs/services-architecture.md §5. Depends on nothing above it.
//
// Barrel stub (Stage 0 scaffold). Populated as services land in Stage 2+.
export {};
