// kernel — the composition root: boots and assembles services + policies + transports
// into the published Astromech package. It composes; it does not conduct. May import
// from any layer. See specs/services-architecture.md §2 (kernel) / §4 decision 6.
//
// Barrel stub (Stage 0 scaffold). Populated as boot/compose code lands (Stage 1+).
export {};
