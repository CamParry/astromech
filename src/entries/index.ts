/** `entries` domain module — entry CRUD service, storage, and scheduled jobs. */
export { entries, initServerContext } from './service.js';
export { registerBuiltInEntryJobs } from './jobs/index.js';
