// transport/local — the Local API: assembles services into the ergonomic nested
// importable Astromech object (in-process; trusted, composes no permissions wrapper).
// Barrel stub (Stage 0 scaffold).
export {};
