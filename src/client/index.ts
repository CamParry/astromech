// client — the fetch Client: consumer of the HTTP API, mirroring the Local API 1:1.
// Talks over the wire only; must NOT import services/policies/storage directly.
// The admin SPA holds this. See specs/services-architecture.md §5.
//
// Barrel stub (Stage 0 scaffold). Populated in Stage 2 (moved from sdk/fetch).
export {};
